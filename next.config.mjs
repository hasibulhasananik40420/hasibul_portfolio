/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'img.freepik.com', // Add Freepik's domain
          
        ],
      },
};

export default nextConfig;
